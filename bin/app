#! /usr/bin/env zsh
. ~/.e/lib/curenv.sh
. ~/.e/lib/setups.sh
if [[ "$1" == "-a" ]] || [[ "$1" == "--all" ]]; then 
    [[ ! "$PWD" =~ "$CENVROOT"$ ]] && usage
    shift
    setup_all $1 $CENVROOT 
else
    [[ ! "$PWD" =~ "$CENVROOT/app/*/" ]] && usage
    setup_one "$@"
fi

