wcheatsheet=$(cat<<'EOF'
* Show vim setting
  - :set all 
  - :map
* `f`ind/`t`ill char in current line: `fFtT` `;` `c`    
* yank to clipboard:  `"*y`
  - `"` to select register"
  - `*` is system register 


EOF
)
