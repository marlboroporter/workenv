wcheatsheet=$(cat<<'EOF'
1. Show vim setting
  - :set all 
  - :map
1. `f`ind/`t`ill char in current line: `fFtT` `;` `c`    
1. yank to clipboard:  `"*y`
  - " to select register"
  - `*` is system register 


EOF
)
